package org.kevoree.kevscript.statement;

import org.kevoree.ContainerRoot;
import org.kevoree.kevscript.Type;
import org.waxeye.ast.IAST;

/**
 * Created by leiko on 12/15/16.
 */
public class AddRepoStmt {

    public static void interpret(IAST<Type> stmt, ContainerRoot model) {
//        final Repository repo = factory.createRepository();
//        repo.setUrl(stmt.getChildren().get(0).childrenAsString());
//        model.addRepositories(repo);
        // DEPRECATED
    }
}
