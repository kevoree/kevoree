<project>

       <modelVersion>4.0.0</modelVersion>
      <groupId>org.kevoree.nativeN</groupId>
      <artifactId>org.kevoree.nativeN.faceDetection_native-osx</artifactId>
      <name>Kevoree :: Tools :: Native :: C-osx</name>
      <packaging>uexe</packaging>

         <parent>
               <groupId>org.kevoree.nativeN</groupId>
               <artifactId>org.kevoree.nativeN.faceDetection_native</artifactId>
               <version>2.1-SNAPSHOT</version>
           </parent>

    <properties>
        <platform>osx</platform>
        <platform.jdk.include.dir>${java.home}/include</platform.jdk.include.dir>
        <platform.javahOS>macosx</platform.javahOS>
        <platform.define>__DARWIN__</platform.define>
        <platform.shlib>dylib</platform.shlib>
        <platform.jni>jnilib</platform.jni>
    </properties>


    <properties>
        <kevoree.version>1.8.9-SNAPSHOT</kevoree.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>native-maven-plugin</artifactId>
                <extensions>true</extensions>

                <configuration>
                    <compilerProvider>generic-classic</compilerProvider>
                    <compilerExecutable>g++</compilerExecutable>
                    <linkerExecutable>g++</linkerExecutable>

                    <compilerStartOptions>
                        <compilerStartOption>-W -O2 -Wall -g  -I.</compilerStartOption>
                    </compilerStartOptions>

                    <sources>
                        <source>
                    <directory>../src/main/c</directory>
                            <includes>
                                <include>**/*.c</include>
                                <include>**/*.cpp</include>
                            </includes>
                        </source>
                    </sources>
                    <linkerEndOptions>
                        <linkerEndOption> -lpthread</linkerEndOption>
                    </linkerEndOptions>
                </configuration>
            </plugin>

        </plugins>
    </build>
</project>
